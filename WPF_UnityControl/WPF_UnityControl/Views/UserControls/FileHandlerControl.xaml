<UserControl
    x:Class="WPF_UnityControl.Controls.FileHandlerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF_UnityControl.Controls"
    xmlns:materialDesign="[materialDesign ]http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="350"
    d:DesignWidth="350"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource MaterialDesignRaisedButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource 900Blush}" />
            <Setter Property="Height" Value="50" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1">
            <StackPanel Margin="30">
                <Button Margin="0,10" Content="ファイル読み込み" />
                <Button Margin="0,10" Content="ファイル保存" />
                <Button Margin="0,10" Content="ファイル保存" />
                <Button Margin="0,10" Content="ファイル保存" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
