<UserControl
    x:Class="WPF_UnityControl.Control.UnityOparationsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF_UnityControl.Control"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="600"
    d:DesignWidth="250"
    Background="{StaticResource AppBaseColor}"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="10">
            <Image
                x:Name="imUnityLogo"
                Height="45"
                HorizontalAlignment="Left"
                Source="/Resources/Logo.png" />
            <Button
                x:Name="btConnect"
                Margin="0,20,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btConnect_Click"
                Content="Unity接続" />
            <Button
                x:Name="btSend"
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btSend_Click"
                Content="シーンロード" />
            <Button
                x:Name="btFetchScene"
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーンデータ取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーンデータ保存" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="データ送信" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="クリア" />

            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
            <Button
                Margin="0,10,0,0"
                Background="{StaticResource AppBaseColor}"
                Click="btFetchScene_Click"
                Content="シーン一覧取得" />
        </StackPanel>
    </Grid>
</UserControl>
